

export class TrackModel {

  constructor(name, number, uri, length, album, artists) {
    this.name = name
    this.number = number
    this.uri = uri
    this.length = length
    this.album = album
    this.artists = artists
  }
}
